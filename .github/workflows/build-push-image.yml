name: Build & Push

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@51e8cf88319b348bf989b5def89d27be0a5d7674 # v2024.10.01
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
