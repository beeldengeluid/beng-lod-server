name: Build & Push

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@c979a600e47b90e04e771f4be4eb236018ba3f15 # v2024.08.06
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
