name: Check Project

on:
  push:

jobs:
  check-project:
    uses: beeldengeluid/gha-workflows/.github/workflows/check-poetry-project.yml@bede4a84f512e0b39c9f063b42ef786a8c305dab # v2026.07.09
    with:
      poetry-version: 2.1.4
    permissions:
      contents: read
