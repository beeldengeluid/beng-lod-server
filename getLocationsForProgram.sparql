#+ summary: Query to get all locations associated with a program
#+ endpoint: http://zorin.beeldengeluid.nl:3020/sparql/

PREFIX schema: <http://data.rdlabs.beeldengeluid.nl/schema/> 
SELECT DISTINCT ?location WHERE
{
?_o1_iri schema:location ?location
}
ORDER BY ?location
